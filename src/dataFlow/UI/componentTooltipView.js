define([
    "jquery",
    "underscore",

],function(
    $,
    _
){
    var tooltipHtml = "<div>Hello this is some text</div>";
    var tooltipTemplate = _.template(tooltipHtml);

    // This is just a wrapper for bootstrap tooltip
});
